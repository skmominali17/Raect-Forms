# React-Forms
Yup for authentication
install it using npm 
import { useForm } from "react-hook-form" --> module
import * as yup from "yup"; --> package
import {yupResolver} from "@hookform/resolvers/yup" 
